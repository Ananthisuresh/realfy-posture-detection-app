services:
  - type: web
    name: realfy-posture-detection-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: "python app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
